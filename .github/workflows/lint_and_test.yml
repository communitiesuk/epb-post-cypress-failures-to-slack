name: Lint and test

on:
  pull_request:
  push:
    branches:
      - main
      - 'v1'

jobs:
  # unit tests
  units:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - run: npm ci
    - run: npm run lint
    - run: npm test
